此模型为2D所有测试案例中最为顺畅的一个，是根据example中的bubble.c直接更改而来的

# 4.13工作日志
(1)检测所画网格质量，加入gird函数，网格十分粗糙，怀疑refine函数是否被真正调用
：检测结果表明refine函数确实被调用，且调用是成功的

(2)解决网格密化只在气泡内部出现的问题，要将细密化网格延伸到距离边界层一定距离的更大圆圈内
：问题解决，通过将refine(phi[] < 0)更改到refine(phi[] < 0.5)

(3)将网格更新，使得边界层的网格更加细密，而气泡中心网格略微粗糙
：方法同上

(4)该网格下核时估算大概为6300

# 4.14工作日志
(1)网格密度必须要注意，文章中网格密度最大值为80个网格每一气泡直径

(2)经过网格密度调整，计算速度明显提升，但还是没有弄清basilisk的具体网格划分方法，例如wave_let函数

(3)网格密度大约在25左右时，最后导致P出现收敛问题

(4)网格密度大概在25左右时，发现左右两列气泡的间距过宽，二者没有相互影响，应考虑减小间距
：间距减小，融合出现

# 4.17工作日志
(1)之前设定的尾迹以及气泡内部的网格过密，应当稀疏，特引入grad_u数据用于处理adapt_wavelet

(2)添加自适应函数后尾迹还是没有解决，气泡内部网格解决

(3)有关two_phase.h以及vof.h的头文件阅读

